>
+[>

  ![.
    >++++++++[<---------->-]<
    >+<[
      = IF NOT 'P'
      >++[<+++++++>-]<+
      >+<
      [
        = IF NOT ':'
        >>+++++[<++++++>-]<-[<++>-]<
        @include(skip_line_socket.b)
      ]>[
        = IF ':'
        @include(check_for_cmds.b)
      ]<
    ]>[
      = IF 'P'
      ![.
        >++++++[>+++[<<---->>-]<-]<-
        >+<[
          = IF NOT 'I'
          >+++++[>+++[<<++++>>-]<-]<+
          @include(skip_line_socket.b)
        ]>[
          = IF 'I'
          ![.
            >>++++[<++++++>-]<++[<--->-]<
            >+<[
              = IF NOT 'N'
              >-<
              >>++++[<++++++>-]<++[<+++>-]<
              @include(skip_line_socket.b)
            ]>[
              = IF 'N'
              ![.
                >+++++[>+++++++[<<-->>-]<-]<-
                >+<[
                  = IF NOT 'G'
                  >++++[>+++++++[<<++>>-]<-]<+
                  @include(skip_line_socket.b)
                ]>[
                  = IF 'G'
                  ![.
                    >++++[>++++[<<-->>-]<-]<
                    >+<[
                      = IF NOT ' '
                      >+++[>++++[<<++>>-]<-]<
                      @include(skip_line_socket.b)
                    ]>[
                      = IF ' '
                      @include(pong.b)
                    ]<
                  ]
                ]<
              ]
            ]<
          ]
        ]<
      ]
    ]<
  ]


<]
<
